/**
 * generateAiText.ts
 *
 * Placeholder for a future utility that generates AI text via the Vercel AI SDK
 * (streamText / generateText) as an alternative to the OpenAI Agents SDK path.
 *
 * Currently unused — the primary AI call path is in convex/chat/actions.ts
 * using the @openai/agents SDK.
 */
